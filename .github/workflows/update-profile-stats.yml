# Disabled: github-readme-stats API is too unreliable for this profile.
# Stats / top-langs / trophies were removed from README.md.
name: Update profile stats images (disabled)

on:
  workflow_dispatch:

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Profile README no longer uses cached stats SVGs."
